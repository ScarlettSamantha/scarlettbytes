{% extends 'base.j2' %}

{% block content %}
<div class="cards-grid">
    <section class="card">
        <h2 class="card-title">Identity & docs</h2>
        <ul class="link-list">
            <li class="link-row">
                <a class="link-primary" target="_blank" href="{{ url_for('gpg') }}">GPG public key</a>
            </li>
            <li class="link-row">
                <a class="link-primary" target="_blank" href="{{ url_for('cv') }}">Curriculum Vitae (CV)</a>
                <span class="link-meta">
                    <a class="link-pill" href="{{ url_for('cv_download') }}">file</a>
                    <a class="link-pill" href="{{ url_for('cv_download_key') }}">signature</a>
                </span>
            </li>
        </ul>
    </section>

    <section class="card">
        <h2 class="card-title">Projects & tools</h2>
        <ul class="link-list">
            <li class="link-row">
                <a class="link-primary" target="_blank" href="{{ url_for('lshw_parser') }}">lshw-parser</a>
                <span class="link-meta">
                    <a class="link-pill" target="_blank" href="{{ url_for('lshw_parser_download') }}">
                        latest zip
                    </a>
                </span>
            </li>
            <li class="link-row">
                <a class="link-primary" target="_blank" href="{{ url_for('openciv') }}">OpenCiv</a>
                <span class="link-meta">
                    <a class="link-pill" target="_blank" href="{{ url_for('openciv_download') }}">
                        latest zip
                    </a>
                </span>
            </li>
            <li class="link-row">
                <a class="link-primary" target="_blank" href="{{ url_for('json_inspector') }}">Json Inspector</a>
                <span class="link-meta">
                    <a class="link-pill" target="_blank" href="{{ url_for('json_inspector_download') }}">
                        latest zip
                    </a>
                </span>
            </li>
        </ul>
    </section>

    <section class="card">
        <h2 class="card-title">Latest commits</h2>
        <ul class="commit-list">
            {% for commit in recent_commits %}
            <li class="commit-row">
                <a class="commit-message" href="{{ commit.url }}" target="_blank" rel="noreferrer">
                    {{ commit.message }}
                </a>
                <span class="commit-meta mono">
                    <a class="link-inline" href="https://github.com/{{ commit.repo }}" target="_blank" rel="noreferrer">
                        {{ commit.repo }}
                    </a>
                    <span class="push-right">{{ commit.sha }} · {{ commit.date }}</span>
                </span>
            </li>
            {% else %}
            <li class="commit-row">
                <span class="commit-meta">Unable to load recent commits right now.</span>
            </li>
            {% endfor %}
        </ul>
    </section>

    <section class="card">
        <h2 class="card-title">Latest posts</h2>
        <ul class="post-list">
            {% for post in recent_posts %}
            <li class="post-row">
                <a class="post-title" href="{{ url_for('blog_post', slug=post.slug) }}">
                    {{ post.title }}
                </a>
                <span class="post-meta mono">
                    {{ post.date }}
                </span>
                <p class="post-summary">
                    {{ post.summary }}
                </p>
            </li>
            {% else %}
            <li class="post-row">
                <span class="post-meta">No blog posts yet.</span>
            </li>
            {% endfor %}
        </ul>
    </section>

    <section class="card card-accent">
        <h2 class="card-title">Contact boundaries</h2>
        <p class="card-text">
            I do not appreciate unsolicited contact from bots or external recruiters.
        </p>
        <p class="card-text">
            For legitimate inquiries you can find my email via WHOIS:
            <a class="link-inline" href="https://www.sidn.nl/whois?q=scarlettbytes.nl" target="_blank">
                Whois☍
            </a>.
        </p>
        <p class="card-text">
            Please be a human, be specific, and skip the generic “exciting opportunity” templates.
        </p>
    </section>
</div>
{% endblock %}